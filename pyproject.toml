# AUTO-GENERATED BY release.py — DO NOT EDIT
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-interactor"
version = "0.1.2.5"
description = "Universal AI interaction library with session and tool support."
readme = { file = "README.md", content-type = "text/markdown" }
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [
  { name = "Wadih Khairallah", email = "woodyk@gmail.com" }
]
keywords = ["text", "extraction", "pii", "analysis"]
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
]
dependencies = [
  "aiohttp",
  "openai",
  "rich",
  "tiktoken",
  "anthropic",
  "requests",
]

[project.urls]
Homepage = "https://github.com/woodyk/interactor"

[project.scripts]

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["interactor", "interactor.*"]
